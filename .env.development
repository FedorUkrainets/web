# По умолчанию dev-режим фронта ходит в деплой на Railway.
# Если хотите работать с локальным бэкендом — создайте файл .env.development.local
# с содержимым: VITE_API_URL=http://localhost:3001/api
# (Vite подтянет .local-файл с приоритетом, он уже в .gitignore.)
VITE_API_URL=https://web-production-948f5.up.railway.app/api
